---
phase: 01-supabase-auth-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
user_setup: []
must_haves:
  truths:
    - User can sign in with email/password via Supabase Auth and see dashboard
    - User session persists across browser refresh
    - User can log out from any page
  artifacts:
    - path: "src/app/login/page.tsx"
      provides: "Login form with email/password authentication"
    - path: "src/contexts/auth-context.tsx"
      provides: "Auth context with login/logout functions"
    - path: "src/lib/auth-helper.ts"
      provides: "Server-side session handling"
    - path: "src/middleware.ts"
      provides: "Auth middleware for route protection"
  key_links:
    - from: "src/app/login/page.tsx"
      to: "src/contexts/auth-context.tsx"
      via: "useAuth hook"
      pattern: "login.*email.*password"
    - from: "src/contexts/auth-context.tsx"
      to: "src/middleware.ts"
      via: "Supabase session cookie"
      pattern: "supabase.auth.getSession"
---

<objective>
Verify Supabase Auth foundation implementation works correctly. All auth components already exist in the codebase - this plan verifies they work end-to-end.
</objective>

<execution_context>
@C:/Users/User/Desktop/claude2/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/User/Desktop/claude2/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-supabase-auth-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Login Flow</name>
  <files>src/app/login/page.tsx, src/contexts/auth-context.tsx</files>
  <read_first>src/app/login/page.tsx, src/contexts/auth-context.tsx</read_first>
  <action>
1. Read login form at src/app/login/page.tsx:
   - Verify form has email/password inputs
   - Verify onSubmit calls login(email, password) from useAuth
   - Verify on success redirects to /dashboard

2. Read auth context at src/contexts/auth-context.tsx:
   - Verify login function uses supabase.auth.signInWithPassword
   - Verify login fetches employee data after sign-in
   - Verify error handling returns proper error

3. Read src/app/api/auth/employee/route.ts:
   - Verify returns employee data with permissions
</action>
  <verify>
    <automated>grep -l "signInWithPassword" src/contexts/auth-context.tsx && grep -l "fetchEmployee" src/contexts/auth-context.tsx</automated>
  </verify>
  <done>Login form uses email/password, calls supabase.auth.signInWithPassword, redirects to dashboard on success</done>
</task>

<task type="auto">
  <name>Task 2: Verify Session Persistence</name>
  <files>src/contexts/auth-context.tsx, src/middleware.ts</files>
  <read_first>src/contexts/auth-context.tsx, src/middleware.ts</read_first>
  <action>
1. Read auth-context.tsx:
   - Verify useEffect calls supabase.auth.getSession() on mount
   - Verify onAuthStateChange listener handles SIGNED_IN/SIGNED_OUT
   - Verify session stored in user state

2. Read middleware.ts:
   - Verify creates Supabase server client with cookies
   - Verify getUser() called to check authentication
   - Verify cookies properly read/written

3. Verify Supabase cookie configuration:
   - Check NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY are set
</action>
  <verify>
    <automated>grep -l "getSession" src/contexts/auth-context.tsx && grep -l "onAuthStateChange" src/contexts/auth-context.tsx && grep -l "getUser" src/middleware.ts</automated>
  </verify>
  <done>Session persists via Supabase cookies, middleware reads cookies correctly</done>
</task>

<task type="auto">
  <name>Task 3: Verify Logout Flow</name>
  <files>src/contexts/auth-context.tsx, src/app/api/auth/logout/route.ts</files>
  <read_first>src/contexts/auth-context.tsx, src/app/api/auth/logout/route.ts</read_first>
  <action>
1. Read auth-context.tsx:
   - Verify logout function exists
   - Verify calls supabase.auth.signOut()
   - Verify clears employee, permissions, user state
   - Verify clears React Query cache

2. Read logout API at src/app/api/auth/logout/route.ts:
   - Verify handles POST
   - Verify calls signOut on server side
</action>
  <verify>
    <automated>grep -l "signOut" src/contexts/auth-context.tsx && test -f src/app/api/auth/logout/route.ts</automated>
  </verify>
  <done>Logout clears all session state, redirects to login</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→Supabase | Email/password sent to Supabase Auth — untrusted input |
| browser→middleware | Session cookie — untrusted, can be forged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | Login form | accept | Supabase handles credentials securely |
| T-01-02 | Tampering | Cookie manipulation | accept | Supabase cookie signing prevents tampering |
</threat_model>

<verification>
Run dev server and test login/logout flow manually, or use automated tests.
</verification>

<success_criteria>
- [ ] Login form accepts email/password, calls Supabase Auth
- [ ] Session persists across page refresh via cookies
- [ ] Logout clears session state
</success_criteria>

<output>
After completion, create `.planning/phases/01-supabase-auth-foundation/01-01-SUMMARY.md`
</output>