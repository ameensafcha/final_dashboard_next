---
phase: 02-auth-middleware-protection
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - AUTH-04
user_setup: []
must_haves:
  truths:
    - Unauthenticated user trying to access any protected route is redirected to /login
    - After successful login, user is redirected to intended destination
  artifacts:
    - path: "src/lib/auth-rbac.ts"
      status: "delete"
    - path: "src/lib/permissions.ts"
      status: "delete"
    - path: "src/middleware.ts"
      provides: "Auth middleware for route protection"
---

<objective>
Delete auth-rbac.ts and permissions.ts as per context. Verify middleware redirects work correctly.
</objective>

<execution_context>
@C:/Users/User/Desktop/claude2/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/User/Desktop/claude2/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-auth-middleware-protection/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete auth-rbac.ts</name>
  <files>src/lib/auth-rbac.ts</files>
  <action>
1. Verify src/lib/auth-rbac.ts exists:
   - Check file contents for hardcoded role checks
   - Document what functions it provides

2. Delete the file:
   - rm src/lib/auth-rbac.ts

3. Check for imports that reference this file:
   - grep -r "auth-rbac" src/ --include="*.ts" --include="*.tsx"
   - Update any imports if needed
  </action>
  <verify>
    <automated>test ! -f src/lib/auth-rbac.ts</automated>
  </verify>
  <done>auth-rbac.ts deleted</done>
</task>

<task type="auto">
  <name>Task 2: Delete permissions.ts</name>
  <files>src/lib/permissions.ts</files>
  <action>
1. Verify src/lib/permissions.ts exists:
   - Check file contents for permission fetching logic
   - Document what functions it provides (will be redesigned in Phase 4-5)

2. Delete the file:
   - rm src/lib/permissions.ts

3. Check for imports that reference this file:
   - grep -r "permissions" src/ --include="*.ts" --include="*.tsx"
   - Update any imports if needed
  </action>
  <verify>
    <automated>test ! -f src/lib/permissions.ts</automated>
  </verify>
  <done>permissions.ts deleted (will be redesigned in Phase 4-5)</done>
</task>

<task type="auto">
  <name>Task 3: Verify Middleware Redirects</name>
  <files>src/middleware.ts</files>
  <read_first>src/middleware.ts</read_first>
  <action>
1. Read middleware.ts:
   - Verify getUser() is called to check auth
   - Verify unauthenticated users redirect to /login
   - Verify API routes return 401

2. Verify redirect logic:
   - Line 29-31: Redirects to /login via NextResponse.redirect
   - Line 34-36: Returns 401 JSON for API routes
  </action>
  <verify>
    <automated>grep -l "redirect" src/middleware.ts && grep -l "401" src/middleware.ts</automated>
  </verify>
  <done>Middleware properly redirects unauthenticated users and returns 401 for API</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→middleware | Session cookie — untrusted, can be forged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|----------------|
| T-02-01 | Tampering | Cookie forgery | accept | Supabase cookie signing prevents tampering |

</threat_model>

<verification>
Run dev server and test unauthenticated access to protected route, or verify via code review.
</verification>

<success_criteria>
- [ ] auth-rbac.ts deleted from src/lib/
- [ ] permissions.ts deleted from src/lib/
- [ ] Middleware redirects unauthenticated users to /login
- [ ] API routes return 401 for unauthenticated access
- [ ] No broken imports after deletion
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-middleware-protection/02-01-SUMMARY.md`
</output>