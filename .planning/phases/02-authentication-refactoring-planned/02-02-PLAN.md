---
phase: 02
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/app-sidebar.tsx
autonomous: true
requirements:
  - AUTH-01
must_haves:
  truths:
    - "Sidebar menu items are filtered by user permissions"
    - "Admin sees all menu items"
    - "Non-admin users see only permitted items"
  artifacts:
    - path: src/components/app-sidebar.tsx
      provides: Dynamic sidebar rendering based on permissions
  key_links:
    - from: src/components/app-sidebar.tsx
      to: src/contexts/auth-context.tsx
      via: useAuth() hook
---

<objective>
Implement dynamic sidebar that renders menu items based on user permissions.

Purpose: Users should only see navigation items for modules they have permission to access. This requires mapping each menu item to a permission string and filtering based on the authenticated user's permissions.

Output: Updated app-sidebar.tsx with permission-based filtering
</objective>

<execution_context>
@C:/Users/User/Desktop/claude2/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/User/Desktop/claude2/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md

# Existing patterns from codebase

From src/components/app-sidebar.tsx (current - all items hardcoded):
```typescript
const inventoryItems = [
  { label: "Raw Materials", href: "/inventory/raw-materials", icon: Package },
  { label: "Stocks", href: "/inventory/stocks", icon: BarChart3 },
  { label: "Receiving", href: "/inventory/receiving", icon: ArrowDownLeft },
];
```

From src/contexts/auth-context.tsx (permissions already added):
```typescript
const { user, employee, role, isAdmin, permissions, isLoading, login, logout } = useAuth();
```

Permission strings from role_permissions table should map to menu items. Need to define a permission mapping (e.g., "inventory:read" for Inventory section).
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Define permission-to-menu mapping</name>
  <files>src/components/app-sidebar.tsx</files>
  <read_first>
    - src/components/app-sidebar.tsx (current menu structure)
    - src/contexts/auth-context.tsx (permissions array)
  </read_first>
  <action>
Create a PERMISSION_MAP constant that maps permission strings to menu items. Define permissions for each section:
- Dashboard: no permission required (always visible)
- Inventory: "inventory:read" permission
- Products: "products:read" permission
- Production: "production:read" permission
- Finance: "finance:read" permission
- Tasks: "tasks:read" permission
- Admin Panel: "admin:access" permission

This maps each sidebar section to a permission string stored in the database role_permissions table.
  </action>
  <acceptance_criteria>
    - PERMISSION_MAP constant defined in app-sidebar.tsx
    - Each menu section mapped to a permission string
  </acceptance_criteria>
  <verify>
    <automated>grep -n "PERMISSION_MAP" src/components/app-sidebar.tsx</automated>
  </verify>
  <done>Permission mapping defined for all menu sections</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Filter menu items by permissions</name>
  <files>src/components/app-sidebar.tsx</files>
  <read_first>
    - src/components/app-sidebar.tsx (with PERMISSION_MAP)
  </read_first>
  <action>
Update the sidebar rendering logic to check permissions before showing each section. Use the useAuth hook's permissions array and isAdmin flag. For each menu section: if user isAdmin, show all sections; otherwise, check if the user has the required permission (e.g., permissions.includes("inventory:read")). Hide sections the user doesn't have permission for. Keep the Tasks section and Admin Panel link conditional on isLoggedIn as before, but also check permissions.
  </action>
  <acceptance_criteria>
    - Sidebar hides sections when user lacks permission
    - Admin users see all sections
    - isAdmin flag bypasses permission check
  </acceptance_criteria>
  <verify>
    <automated>grep -n "permissions.includes\|isAdmin" src/components/app-sidebar.tsx | head -10</automated>
  </verify>
  <done>Sidebar filters menu items based on user permissions</done>
</task>

</tasks>

<verification>
- Build passes: npm run build
- TypeScript check: npx tsc --noEmit
</verification>

<success_criteria>
- Admin users see all sidebar sections
- Non-admin users only see sections for which they have permissions
- Sidebar renders without errors for all user types
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication-refactoring-planned/02-02-SUMMARY.md`
</output>