---
phase: "02"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: ["src/app/admin/roles/page.tsx", "src/app/admin/roles/roles.module.css", "src/components/RoleForm.tsx"]
autonomous: true
requirements: ["FUNC-2.1"]
must_haves:
  truths:
    - "Role list is rendered using React Server Components on initial load"
    - "Role management uses Vanilla CSS for all styling (no utility classes)"
    - "Roles can be added and edited via a dynamic form"
  artifacts:
    - path: "src/app/admin/roles/page.tsx"
      provides: "Role management entry point (RSC)"
    - path: "src/app/admin/roles/roles.module.css"
      provides: "Vanilla CSS styles for role management"
    - path: "src/components/RoleForm.tsx"
      provides: "Client component for adding/editing roles"
  key_links:
    - from: "src/app/admin/roles/page.tsx"
      to: "src/lib/prisma.ts"
      via: "Direct database fetch (RSC)"
---

<objective>
Refactor the existing Role Management UI to use React Server Components for data fetching and Vanilla CSS for styling, as per project preferences.

Purpose: Foundation for dynamic role management using database-backed roles.
Output: RSC-powered Role management page with Vanilla CSS styling.
</objective>

<execution_context>
@C:/Users/User/Desktop/claude2/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/User/Desktop/claude2/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
@src/app/api/roles/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Vanilla CSS module for Role Management</name>
  <files>src/app/admin/roles/roles.module.css</files>
  <action>
    Create a Vanilla CSS module for the role management UI. 
    Include styles for:
    - Page container (max-width, centering)
    - Headers (typography, colors)
    - Role list table/grid (borders, spacing, hover states)
    - Action buttons (Add, Edit, Delete)
    - Form inputs and labels
    Avoid using Tailwind utility classes. Use colors that match the project theme (Amber/Slate) as per D-01/D-02 if applicable.
  </action>
  <verify>
    File exists and contains valid CSS classes used in subsequent tasks.
  </verify>
  <done>src/app/admin/roles/roles.module.css created</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement RoleForm Client Component</name>
  <files>src/components/RoleForm.tsx</files>
  <behavior>
    - Component accepts `onSuccess` and `initialData` props.
    - Handles form submission to `/api/roles` (POST for new, PUT for edit).
    - Validates that Role Name is not empty.
    - Uses Vanilla CSS from src/app/admin/roles/roles.module.css (via imports or passed classes).
  </behavior>
  <action>
    Create RoleForm.tsx as a "use client" component.
    Use standard fetch() for API calls.
    After successful submission, call `onSuccess()` to allow the parent (RSC) to re-sync via `router.refresh()`.
  </action>
  <verify>
    <automated>npm test src/components/RoleForm.test.tsx</automated>
  </verify>
  <done>RoleForm.tsx implemented with Vanilla CSS and successful API integration</done>
</task>

<task type="auto">
  <name>Task 3: Refactor Roles Page to RSC</name>
  <files>src/app/admin/roles/page.tsx</files>
  <action>
    Convert src/app/admin/roles/page.tsx from a Client Component to a Server Component.
    - Fetch roles directly using `prisma.role.findMany()`.
    - Import and use `roles.module.css`.
    - Render the Role List.
    - Use RoleForm (Client Component) for adding/editing roles.
    - Handle Delete via a simple Client Component or button that calls `/api/roles`.
  </action>
  <verify>
    Navigate to /admin/roles and verify the list is rendered (SSR/RSC) and styling is applied via Vanilla CSS.
  </verify>
  <done>Roles page refactored to RSC with Vanilla CSS</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | Client-side form submission to `/api/roles` must be authenticated and authorized. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | `/api/roles` | Mitigate | Ensure API route enforces `requireAdminApi` (already implemented in Phase 1). |
| T-02-01-02 | Information Disclosure | `src/app/admin/roles/page.tsx` | Mitigate | Ensure page is wrapped in Admin Layout/Guard (already exists in src/app/admin/layout.tsx). |
</threat_model>

<verification>
Verify role creation, listing, and editing.
Ensure no Tailwind classes are used in the new components.
</verification>

<success_criteria>
- Role Management page renders server-side.
- Styles are applied via Vanilla CSS module.
- Roles can be Added, Edited, and Deleted using database-backed APIs.
</success_criteria>

<output>
After completion, create `.planning/phases/02-management-ui/02-01-SUMMARY.md`
</output>
