---
phase: "02"
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: ["src/app/admin/roles/permissions/page.tsx", "src/app/admin/roles/permissions/matrix.module.css", "src/components/PermissionMatrix.tsx"]
autonomous: true
requirements: ["FUNC-2.2", "FUNC-2.2-REVAL"]
must_haves:
  truths:
    - "Permission Matrix displays all Roles as columns and all Permissions as rows"
    - "Permissions are grouped by resource for improved readability"
    - "Toggling a permission in the matrix updates the UI immediately and syncs with the database"
  artifacts:
    - path: "src/app/admin/roles/permissions/page.tsx"
      provides: "Matrix page entry point (RSC)"
    - path: "src/app/admin/roles/permissions/matrix.module.css"
      provides: "Vanilla CSS for the matrix grid"
    - path: "src/components/PermissionMatrix.tsx"
      provides: "Interactive matrix component"
  key_links:
    - from: "src/components/PermissionMatrix.tsx"
      to: "/api/roles/permissions"
      via: "POST request for toggling"
---

<objective>
Implement the core Permission Matrix UI, allowing administrators to manage permissions across all roles in a single view.

Purpose: Efficient cross-role permission management.
Output: Dynamic Permission Matrix UI grouped by resource.
</objective>

<execution_context>
@C:/Users/User/Desktop/claude2/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/User/Desktop/claude2/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/api/roles/permissions/route.ts
@src/app/api/permissions/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Matrix Grid styles (Vanilla CSS)</name>
  <files>src/app/admin/roles/permissions/matrix.module.css</files>
  <action>
    Define the CSS grid for the Permission Matrix.
    Requirements:
    - Sticky headers for roles (columns).
    - Sticky first column for permission labels (rows).
    - Resource grouping headers that span across all columns.
    - Checkbox/Toggle styling for matrix cells.
    - Responsive handling (horizontal scroll for many roles).
  </action>
  <verify>
    File exists and contains classes for .matrixContainer, .matrixHeader, .matrixCell, .groupHeader.
  </verify>
  <done>src/app/admin/roles/permissions/matrix.module.css created</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement PermissionMatrix Client Component</name>
  <files>src/components/PermissionMatrix.tsx</files>
  <behavior>
    - Component receives `roles` (with nested permissions) and `allPermissions` as props.
    - Renders a grid based on matrix.module.css.
    - Groups rows by Permission Resource.
    - Handles toggle click:
      - Optimistic UI update.
      - Calls `/api/roles/permissions` with { roleId, permission_id, active }.
      - Reverts on error and shows notification.
    - Includes a search filter for permission names.
  </behavior>
  <action>
    Build the Matrix UI using the provided props. 
    Use Vanilla CSS classes from matrix.module.css.
    Implement resource grouping logic (split permission key by ':').
  </action>
  <verify>
    <automated>npm test src/components/PermissionMatrix.test.tsx</automated>
  </verify>
  <done>PermissionMatrix.tsx implemented with optimistic updates and grouping</done>
</task>

<task type="auto">
  <name>Task 3: Update Permissions Page to RSC</name>
  <files>src/app/admin/roles/permissions/page.tsx</files>
  <action>
    Overwrite src/app/admin/roles/permissions/page.tsx to be a Server Component.
    - Fetch all roles (including permissions) and all permission definitions from the database.
    - Pass data to PermissionMatrix.tsx.
    - Wrap in a layout with a back link to Roles Management.
  </action>
  <verify>
    Navigate to /admin/roles/permissions and verify the Matrix is rendered with all roles and grouped permissions.
  </verify>
  <done>Permissions page converted to Matrix view (RSC)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Matrix UI -> API | Bulk or individual toggles sent to `/api/roles/permissions`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Tampering | `/api/roles/permissions` | Mitigate | Enforce `requireAdminApi` and validate that roleId and permission_id exist in DB. |
| T-02-02-02 | Denial of Service | Permission Matrix | Mitigate | Implement simple search/filter and pagination if perms exceed 100+ (though current count is ~21). |
</threat_model>

<verification>
Verify cross-role permission toggling.
Ensure permissions are correctly grouped by resource.
Verify real-time feedback (UI updates instantly, persists on reload).
</verification>

<success_criteria>
- Permission Matrix functional and grouped by resource.
- RSC fetching roles/permissions successfully.
- Optimistic updates for role-permission toggles.
</success_criteria>

<output>
After completion, create `.planning/phases/02-management-ui/02-02-SUMMARY.md`
</output>
