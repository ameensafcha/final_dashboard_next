---
phase: "02"
plan: "03"
type: execute
wave: 3
depends_on: ["02-02"]
files_modified: ["src/app/admin/employees/page.tsx", "src/app/admin/employees/employees.module.css", "src/app/admin/roles/page.tsx"]
autonomous: true
requirements: ["FUNC-2.3-USER"]
must_haves:
  truths:
    - "Employee management UI uses Vanilla CSS for all components"
    - "Administrators can assign database-backed roles to employees via a dropdown"
    - "Role assignments trigger cache revalidation for the assigned employee's permissions"
  artifacts:
    - path: "src/app/admin/employees/employees.module.css"
      provides: "Vanilla CSS styles for employee management"
  key_links:
    - from: "src/app/admin/employees/page.tsx"
      to: "/api/auth/role"
      via: "PATCH request for role assignment"
---

<objective>
Update the Employee Management UI to support database-driven role assignment and refactor it to use Vanilla CSS for consistency.

Purpose: Finalize user-role integration.
Output: Refactored Employee Management UI with functional role assignment.
</objective>

<execution_context>
@C:/Users/User/Desktop/claude2/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/User/Desktop/claude2/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/api/auth/role/route.ts
@src/app/api/employees/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Employee Management Styles (Vanilla CSS)</name>
  <files>src/app/admin/employees/employees.module.css</files>
  <action>
    Define Vanilla CSS classes for the employee management table and forms.
    Include styles for:
    - Table layout (header, rows, cells)
    - Role selection dropdown
    - Status badges (Active/Inactive)
    - Action links (Edit, Deactivate)
    Maintain the project's visual theme.
  </action>
  <verify>
    File exists and contains classes used in Task 2.
  </verify>
  <done>src/app/admin/employees/employees.module.css created</done>
</task>

<task type="auto">
  <name>Task 2: Refactor Employee Page to Vanilla CSS</name>
  <files>src/app/admin/employees/page.tsx</files>
  <action>
    Refactor the existing EmployeesPage (src/app/admin/employees/page.tsx).
    - Remove Tailwind utility classes.
    - Use classes from employees.module.css.
    - Ensure the Role dropdown correctly calls `/api/auth/role` and revalidates queries/tags.
    - Add a "Manage Roles & Permissions" link at the top of the page for easy navigation.
  </action>
  <verify>
    Navigate to /admin/employees and verify role assignment works and the UI follows Vanilla CSS styling.
  </verify>
  <done>Employee management page refactored to Vanilla CSS with functional role assignment</done>
</task>

<task type="auto">
  <name>Task 3: Link Roles to Matrix and Final Verification</name>
  <files>src/app/admin/roles/page.tsx</files>
  <action>
    Add a prominent "Manage Permission Matrix" button/link to the Roles page.
    Perform a final check across:
    - Role List/Form (Plan 01)
    - Permission Matrix (Plan 02)
    - Employee Role Assignment (Plan 03)
    Ensure consistent styling and revalidation logic.
  </action>
  <verify>
    Verify end-to-end flow: Create Role -> Add Permission via Matrix -> Assign Role to Employee -> Employee permissions updated.
  </verify>
  <done>Phase 2 UI integration completed and verified</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin -> Employee Role | Changing an employee's role via the UI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Tampering | `/api/auth/role` | Mitigate | Ensure endpoint validates both employeeId and roleId exist and requires Admin privileges. |
| T-02-03-02 | Information Disclosure | `src/app/admin/employees/page.tsx` | Mitigate | Ensure page is protected by Admin auth (via layout/middleware). |
</threat_model>

<verification>
Verify role assignment for employees.
Verify navigation between Role Management and Permission Matrix.
Final audit for any remaining Tailwind classes in the management UI.
</verification>

<success_criteria>
- Employee management UI is clean and uses Vanilla CSS.
- Roles can be dynamically assigned and effect permissions immediately.
- Integrated navigation between Roles, Matrix, and Employees.
</success_criteria>

<output>
After completion, create `.planning/phases/02-management-ui/02-03-SUMMARY.md`
</output>
