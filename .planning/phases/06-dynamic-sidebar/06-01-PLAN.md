---
phase: 06-dynamic-sidebar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/users/permissions/route.ts
  - src/components/app-sidebar.tsx
  - src/hooks/use-user-permissions.ts
autonomous: true
requirements:
  - MOD-02
  - DASH-01

must_haves:
  truths:
    - "All authenticated users can access dashboard"
    - "Sidebar renders only menu items user has permission to view"
    - "User with no module permissions sees only dashboard"
  artifacts:
    - path: "src/app/api/users/permissions/route.ts"
      provides: "Fetches user's permissions from role_permissions table"
      exports: ["GET /api/users/permissions"]
    - path: "src/components/app-sidebar.tsx"
      provides: "Sidebar with permission-based filtering"
      contains: "useQuery for permissions, MENU_PERMISSIONS mapping"
    - path: "src/hooks/use-user-permissions.ts"
      provides: "Custom hook for fetching user permissions"
      exports: ["useUserPermissions"]
  key_links:
    - from: "app-sidebar.tsx"
      to: "/api/users/permissions"
      via: "useQuery in useUserPermissions hook"
      pattern: "useQuery.*\\['user-permissions'\\]"
    - from: "/api/users/permissions"
      to: "role_permissions"
      via: "Prisma query with role_id filter"
      pattern: "prisma\\.role_permissions\\.findMany"

<objective>
Make sidebar display only menu items the current user has permission to access. All authenticated users can access dashboard by default.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-dynamic-sidebar/06-CONTEXT.md
@src/components/app-sidebar.tsx
@src/app/api/roles/permissions/route.ts

From src/lib/auth-helper.ts:
```typescript
export type AuthUser = {
  id: string;
  email: string;
  role: string | null;
  isAdmin: boolean;
};
```

From prisma/schema.prisma:
```prisma
model role_permissions {
  id          String   @id @default(uuid())
  role_id     String   
  permission  String   
  is_active   Boolean  @default(true)
  role        roles    @relation(fields: [role_id], references: [id], onDelete: Cascade)
  @@unique([role_id, permission])
}
```

Menu items from app-sidebar.tsx:
- Dashboard (always accessible - DASH-01)
- Inventory (paths: /raw-materials, /stocks, /receiving)
- Products (paths: /products)
- Tasks (paths: /tasks)
- Admin Panel (admin only - current hardcoded check)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create API to fetch user's permissions</name>
  <files>src/app/api/users/permissions/route.ts</files>
  <action>
Create GET endpoint at /api/users/permissions that:
1. Uses getCurrentUser() from auth-helper to get authenticated user
2. Fetches user's role_id from employees table (includes role relation)
3. Queries role_permissions table for all permissions where role_id matches and is_active=true
4. Returns array of permission strings (e.g., ["edit:dashboard", "view:stocks"])
5. If user has no role or no permissions, returns empty array []

Example response: { "permissions": ["edit:dashboard", "view:stocks", "manage:batches"] }
  </action>
  <verify>
<automated>
curl -X GET http://localhost:3000/api/users/permissions -H "Cookie: sb-access-token=..." 
# Should return permissions array for authenticated user
</automated>
  </verify>
  <done>GET /api/users/permissions returns user's active permissions from role_permissions table</done>
</task>

<task type="auto">
  <name>Task 2: Refactor app-sidebar to filter by permissions</name>
  <files>src/components/app-sidebar.tsx</files>
  <action>
Refactor app-sidebar.tsx to:

1. Import useQuery from @tanstack/react-query
2. Add useUserPermissions hook: fetch permissions from /api/users/permissions
3. Define MENU_PERMISSIONS mapping:
   - Dashboard: [] (no permissions required - DASH-01)
   - Inventory: ["view:raw-materials", "view:stocks", "view:receiving"]
   - Products: ["view:products", "edit:products"]
   - Tasks: ["view:tasks"]
   - Admin Panel: ["manage:roles"] (or check isAdmin separately)

4. Filter menu groups:
   - For each menu group, check if user has ANY of the required permissions
   - Use Array.some() to show group if user has at least one permission
   - If no permissions at all, show only Dashboard (per D-06)

5. Preserve existing sidebar styling and behavior
6. Handle loading state: show skeleton or minimal sidebar while loading
7. Handle error state: if API fails, default to dashboard-only

Key: Keep Dashboard visible for ALL authenticated users per DASH-01 requirement.
  </action>
  <verify>
<automated>
# Login as different users and verify sidebar shows different items
# Check browser console for permission fetch
</automated>
  </verify>
  <done>Sidebar renders only menu items user has permission to view. User with no module permissions sees only Dashboard.</done>
</task>

</tasks>

<verification>
- [ ] GET /api/users/permissions returns user's permissions from DB
- [ ] Sidebar fetches permissions on mount via TanStack Query
- [ ] Dashboard link always visible (DASH-01)
- [ ] Menu groups hidden when user lacks required permissions (MOD-02)
- [ ] Users without module permissions see only Dashboard (D-06 fallback)
</verification>

<success_criteria>
1. All authenticated users can access dashboard (DASH-01 satisfied)
2. Sidebar renders only menu items user has permission to view (MOD-02 satisfied)
3. User with no module permissions sees only dashboard
</success_criteria>

<output>
After completion, create `.planning/phases/06-dynamic-sidebar/06-01-SUMMARY.md`
</output>