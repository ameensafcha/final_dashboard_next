---
phase: 11-fix-build-errors-and-remove-dead-code
plan: "01"
type: execute
wave: "1"
depends_on: []
files_modified:
  - src/app/admin/layout.tsx
  - src/app/api/settings/route.ts
  - src/lib/auth-helper.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "npm run build completes without errors"
    - "Build produces working production bundle"
  artifacts:
    - path: src/app/admin/layout.tsx
      provides: Admin layout without deleted import
    - path: src/app/api/settings/route.ts
      provides: Settings API with valid imports
    - path: src/lib/auth-helper.ts
      provides: requireRole export if added
  key_links:
    - from: src/app/admin/layout.tsx
      to: src/lib/auth-helper.ts
      via: import and function call
      pattern: requireAuth|requireAdmin
    - from: src/app/api/settings/route.ts
      to: src/lib/auth-helper.ts
      via: requireRole import
      pattern: requireRole
---

<objective>
Fix TypeScript build errors preventing successful `npm run build`.
</objective>

<execution_context>
@C:/Users/User/Desktop/claude2/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@C:/Users/User/Desktop/claude2/.planning/phases/11-fix-build-errors-and-remove-dead-code/11-CONTEXT.md
@C:/Users/User/Desktop/claude2/src/lib/auth-helper.ts
@C:/Users/User/Desktop/claude2/src/app/admin/layout.tsx
@C:/Users/User/Desktop/claude2/src/app/api/settings/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix admin layout import from deleted auth-rbac</name>
  <files>src/app/admin/layout.tsx</files>
  <read_first>src/app/admin/layout.tsx, src/lib/auth-helper.ts</read_first>
  <action>
1. Remove import of `checkRoutePermission` from `@/lib/auth-rbac` (file was deleted)
2. Replace route permission check with existing auth helpers from `auth-helper.ts`:
   - Use `requireAuth()` from auth-helper to enforce authentication
   - Use `getCurrentUser()` to check admin status for /admin route access
   
3. Rewrite AdminLayout to:
   - Import { getCurrentUser } from "@/lib/auth-helper" (NOT auth-rbac)
   - Call const user = await getCurrentUser()
   - If !user, redirect("/login")
   - If !user.isAdmin, redirect("/unauthorized") or redirect("/dashboard")
   - Return children normally for authorized admin users
  </action>
  <verify>
    <automated>grep -q "auth-rbac" src/app/admin/layout.tsx && echo "FAIL: still references auth-rbac" || echo "PASS: no auth-rbac import"</automated>
  </verify>
  <done>AdminLayout uses only auth-helper.ts exports, no references to deleted auth-rbac</done>
</task>

<task type="auto">
  <name>Task 2: Fix settings route requireRole import</name>
  <files>src/app/api/settings/route.ts, src/lib/auth-helper.ts</files>
  <read_first>src/app/api/settings/route.ts, src/lib/auth-helper.ts</read_first>
  <action>
1. Add requireRole function to auth-helper.ts that:
   - Accepts role name as parameter (e.g., 'admin', 'viewer')
   - Returns user if they have the required role
   - Returns NextResponse with 403 if role check fails
   - Reuse existing requireAuth logic + role name check

2. Export the new requireRole function from auth-helper.ts

3. Keep settings/route.ts imports - they already expect requireRole
  </action>
  <verify>
    <automated>grep -q "export.*requireRole" src/lib/auth-helper.ts && echo "PASS: requireRole exported" || echo "FAIL: requireRole not exported"</automated>
  </verify>
  <done>settings route has valid requireRole import from auth-helper</done>
</task>

<task type="auto">
  <name>Task 3: Verify build passes</name>
  <files>package.json</files>
  <read_first>package.json</read_first>
  <action>
Run npm run build and verify it completes without errors:
  cd to project root
  npm run build
  Should complete with "Build completed successfully" or similar
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -q "Build error\|failed\|Error:" && echo "FAIL: build has errors" || echo "PASS: build successful"</automated>
  </verify>
  <done>npm run build completes without errors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Untrusted input - handled by existing auth helpers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Denial of Service | build process | accept | Build failures don't affect runtime |
| T-11-02 | Information Disclosure | error messages | accept | Next.js handles appropriately |
</threat_model>

<verification>
Run: npm run build
Expected: Build completes successfully with no errors
</verification>

<success_criteria>
- [ ] src/app/admin/layout.tsx uses only auth-helper.ts exports
- [ ] src/lib/auth-helper.ts exports requireRole function
- [ ] src/app/api/settings/route.ts has valid imports
- [ ] npm run build completes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/11-fix-build-errors-and-remove-dead-code/11-01-SUMMARY.md`
</output>